import { RatingcolorDirective } from './ratingcolor.directive';

describe('RatingcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new RatingcolorDirective();
    expect(directive).toBeTruthy();
  });
});
